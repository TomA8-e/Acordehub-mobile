<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Para acceder a la galería en Android 13+ -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <!-- Para Android 12 e inferior -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AcordeHub"
        tools:replace="android:allowBackup">

        <!-- Login es el punto de entrada -->
        <activity
            android:name=".ui.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".ui.RegisterActivity" android:exported="false"/>

        <!-- MainActivity contiene los fragments (Home, Perfil, etc.) -->
        <activity android:name=".ui.MainActivity" android:exported="false"/>

        <activity android:name=".ui.SpotifyArtistSearchActivity" android:exported="false"/>
        <activity android:name=".ui.PublishProjectActivity" android:exported="false"/>

        <!-- HomeActivity ya no se usa como destino principal, puede eliminarse
             o mantenerse vacía por ahora -->
        <activity android:name=".ui.HomeActivity" android:exported="false"/>

    </application>

</manifest>
